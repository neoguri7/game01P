# Implementer syntax-only compile check — PROVENANCE / CAVEAT

STATUS: **NOT usable as clean-build evidence.**

This file originally listed only the translation units that were fed to a
syntax-only MSVC invocation. Round-2 review found that the file contained no
command line, no output and no exit code, so it could not substantiate the
"clean" claim that `pass-1.md` / `pass-2.md` cited it for.

The check was run by the implementer session with (implementer-reported, not
reproducible here — no MSVC in the reviewer environment):

```text
cl /nologo /std:c++latest /EHsc /permissive- /utf-8 /W4 /Zs <7 TUs>
exit code: reported 0
output:    reported empty (no diagnostics)
```

Translation units reported as checked:

```text
check_all.cpp
AssetManager.cpp
FContentLoader.cpp
GameplayServices.cpp
FUnitContent.cpp
FSkillContent.cpp
FContentRegistry.cpp
```

Caveats that keep this `UNVERIFIED`:

- `/Zs` is syntax-only: it does **not** link and does **not** install the vcpkg
  manifest, so a missing dependency or a link error is invisible to it.
- The command line, exit code and output above are the implementer's report; no
  independent session reproduced them.
- The authoritative build gate remains owner-run:
  `scripts/check-windows.ps1 -Configuration Debug`.
